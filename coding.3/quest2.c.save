#include <stdio.h>
#include <stdlib.h> // exit
#include <sys/wait.h> // wait
#include <unistd.h> // fork

int main(void) {
    int r = fork();
    if (r < 0) { // fork failed
        fprintf(stderr, "fork failed\n");
        exit(1);
    } else if rf == 0) { // child
        printf("hello\n");
    } else { // parent
        wait(NULL);
        printf("goodbye\n");
    }

