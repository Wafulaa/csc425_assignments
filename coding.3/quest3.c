#include <studio.h>
#include <unistd.h>
#include <stdlib.h>
int main(int argc, char* argv[]){
	int rc = fork():
	int status;
	if (rc < 0) {// fork fails
   	fprintf(stderr, "fork failed\n");
	} else if (rc ==0){
	printf("hello\n");
	}else{
	while (waitpid (ret, &statsus, WNOHANG) ==0 {
		sleep(1)
	}
	printf("Goodbye\n");
	}
	return 0;
	}
